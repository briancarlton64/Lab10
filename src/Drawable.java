import java.awt.*;

/**
 * The drawable interface requires that implementing classes
 * have a draw method that would draw the given component.
 */
public interface Drawable {
    void draw(Graphics g);
}
